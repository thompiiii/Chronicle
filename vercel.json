{
  "buildCommand": "cd chronicle-dnd && npm install && npm run build",
  "outputDirectory": "chronicle-dnd/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
